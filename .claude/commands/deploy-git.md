Stage all changes, commit with a concise message describing what changed, and push to GitHub (origin main).
- Never commit .env files
- Use a clear commit message based on what files changed
- Run: git add . && git commit -m "..." && git push
